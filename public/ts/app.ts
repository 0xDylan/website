// github copilot op?

addEventListener('keypress', (event) => {
  const key = event.key
  const word = 'pico'
  let keys = []

  keys.push(key)

  if (keys.join('') === word) {
    window.location.href = 'https://shxrk.xyz'
  }
})
